# v3 설계 구현 기록

기준 문서: `../SCOREFORGE_DEEP_ANALYSIS_AND_IMPROVEMENT_PROMPTS(Fable51).md`
시작 기준: `20a188a` (2026-09-03), 작업 트리 clean.

## 단계별 결과

| 단계 | 반영 내용 | 검증 |
| --- | --- | --- |
| A-0~A-2 | 기존 master/이력 유지, 기준 JSON/XML/MIDI/스크린샷, Node·브라우저 하네스 | 기준 9개 및 데모 4곡 통과 |
| A-3/A-4 | 단축키 충돌, 임시표 배치, 타이·슬러, 믹서 팬, 악기 XML 선언, 저장 상태, 앱 다이얼로그 | 코어·조판·입출력·UI 회귀 |
| B-1~B-3 | 비직렬화 캐시, 이벤트 인덱스, relayout/repaint 분리, 장식 복사, voices/events 호환 | 선택 시 render/자동 저장 0회, 장식/JSON 회귀 |
| B-4/B-5 | app 약 1,020줄, 입력·편집·패널·인증 분리, KEYMAP 단일화, undo 라벨·1초 병합 | 97개 단축키 생성, 연속 편집 undo 검증 |
| C-1~C-4 | 32/64분·겹온·겹점, 마디별 조/박자/음자리표, 알토·테너·8vb, 옥타브 선, 화음 개별 음 | Fraction/음높이/개별 삭제·이동·타이 회귀 |
| C-5/C-6 | 마디 삽입/삭제/분할/합치기/못갖춘마디, 여러 마디 쉼표와 펼침 | 전 보표 길이, 앵커, 접힌 마디 탐색 |
| C-7/C-8 | 트릴·모르덴트·턴과 재생, D.C./D.S./Coda/Fine/To Coda | 점프 제한·반복·실제 세부 재생 이벤트 |
| D-1~D-4 | 실제 페이지, SVG 메타데이터, 텍스트 간격, 스타일, SVG/300dpi PNG/인쇄 | 64마디 4중주 4페이지, 좌표 변환, A4 2480×3508 PNG |
| E-1/E-2 | 다중 파트/보표/성부/잇단음표 MusicXML/MXL, MIDI 파싱·양자화 옵션 | 브라우저 입출력 12개 시나리오 |
| E-3/E-4 | 속도/루프/예비박/페르마타/실시간 믹서, SMF type 1, 로컬 벤더 | 스케줄러·팬/볼륨·드럼 채널·실제 UI 재생 |
| F-1 | 소유자 악보/RLS/충돌/읽기 전용 공유/오프라인 대기/관리자 악보 수 | 원격 SQL 및 실제 로그인·독립 브라우저·익명 공유 검증 |
| F-2 | 서비스 워커/설치 manifest/갱신 확인/60MB 샘플 LRU | 오프라인 새로고침·저장 악보·재생, 캐시 상한 테스트 |
| F-3~F-5 | 모바일 하단 도구·길게 누르기·핀치, KO/EN, 포커스·ARIA | 375/768px 터치, 4테마 axe 위반 0건, 미등록 번역 키 0건 |
| F-6 | CSP/캐시/권한 헤더, 공개 설정, 오류 처리, 버전/벤더 해시 | Vercel 헤더를 적용한 로컬 서버에서 CSP 위반 0건; 원격 확인은 아래 기록 |

## 실행한 검증

- `npm run check`: JS 20개 구문 검사 통과.
- `npm test`: 104개 통과, 실패 0개. 별도 실행하는 브라우저 parent 1개는 기본 명령에서 의도적으로 skip.
- `node --test tests/ui-v3.test.mjs` (Playwright 경로 지정): 13/13 통과. 실제 Edge 편집·다운로드·재생·터치 포함.
- `tests/verify-release.mjs`: 데모 4곡, 입출력 12개, 선택/화음/마디/페이지/공유 읽기 전용/3개 화면 폭/4테마/언어/axe 통합 검증.
- `tests/verify-offline.mjs`: 앱 셸 41개 캐시, Supabase 데이터 캐시 제외, 오프라인 재시작/로컬 악보 복구/재생, CSP/브라우저 예외 0건.
- Headless Edge의 실제 인쇄 경로로 A4 PDF를 생성하고 Poppler로 4페이지임을 확인했다. 렌더된 페이지에서 시스템 분할과 페이지 번호를 확인했다. 물리 프린터 검사는 별도다.
- `tests/verify-cloud-live.mjs`: 실제 로그인, 저장, 독립 브라우저 열기, 충돌 차단, 익명 공유, 공유 해제, 삭제. 임시 계정·악보·프로필 잔여 0개.
- [Supabase 적용·보안 검증](docs/SUPABASE_V3_VERIFICATION.md), [성능 기준·비교](tests/baseline/PERF.md).
- 스크린샷과 상세 JSON은 로컬 `output/playwright/v3/`에 보관하며 배포/커밋에서 제외한다.

## 설계에서 정정한 점

- 저장소는 이미 초기화되어 있으므로 재초기화하지 않았다.
- 겹점 4분음표는 온음표 단위로 **7/16**이다. 설계의 7/8 표기는 정정했다.
- 32/64분 쉼표 SMuFL 코드는 E4E8/E4E9를 사용한다.
- 공유 view만으로 공개 악보 열거를 차단할 수 없어, 정확한 12자 slug 하나만 받는 좁은 RPC로 구현했다.
- 벡터 SVG는 자체 완결 fallback 도형을 기본 사용한다. 포함된 로컬 폰트는 읽기 실패해도 내보내기를 막지 않는다.
- 버전 없는 JS를 1년 immutable로 캐시하지 않는다. HTTP 재검증 + 버전별 서비스 워커 셸을 사용한다.
- Lighthouse PWA 검사는 폐기되었으므로 설치 manifest·서비스 워커·실제 오프라인 재시작을 직접 검사했다. [Chrome 공식 안내](https://developer.chrome.com/docs/lighthouse/pwa/installable-manifest/).

## 범위와 제약

- MusicXML 전용 외부 MuseScore 프로그램과 물리 MIDI 장비/프린터는 실행하지 않았다. MXL은 생성한 압축 fixture 및 DOMParser로 검사했다.
- MusicXML 가져오기는 최대 500마디, 파트당 32보표, 보표당 4성부를 지원한다. 마디 중간의 박자/조표 변경, 폴리미터, 일부 복합 기호는 리포트에 한계를 표시한다.
- MIDI 가져오기는 SMF 0/1과 PPQ를 지원하며 SMPTE/type 2는 명시적으로 거부한다. 양자화와 장식·페르마타 재생이 포함된 MIDI를 원래의 기보와 항상 동일하게 되돌릴 수는 없다.
- 텍스트 폭은 보수적 추정치다. 한 시스템 자체가 한 페이지보다 큰 과도한 편성은 `page.overflow`로 표시한다.
- 첫 조판은 새 기능 때문에 기준보다 느려질 수 있다. 반복 표시 캐시와 선택 repaint의 개선 수치는 첫 조판 비용과 구분해 기록했다.
- 익명 공유 해제는 이후 조회를 막지만 이미 내려받은 내용을 회수하지는 않는다. 기존 프로젝트의 유출 비밀번호 검사 비활성화 경고는 별도 운영 설정으로 남겨두었다.

## 배포

기존 Vercel 프로젝트 `musescore-copy`에 연결된 master를 사용한다. 최종 커밋/원격 배포 확인 결과는 후속 검증 기록으로 갱신한다.
